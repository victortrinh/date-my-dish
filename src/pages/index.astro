---
// Root redirect: detect browser language and redirect to /en/ or /fr/
return Astro.redirect(`/${(Astro.request.headers.get("accept-language") || "").toLowerCase().startsWith("fr") ? "fr" : "en"}/`, 302);
---
