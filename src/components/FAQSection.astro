---
import { t, type Locale } from "@i18n/utils";

interface FAQ {
  question: string;
  answer: string;
}

interface Props {
  locale: Locale;
  faqs: FAQ[];
}

const { locale, faqs } = Astro.props;
---

{faqs.length > 0 && (
  <section class="no-print mt-12 mb-8">
    <h2 class="mb-6 font-heading text-2xl font-bold text-neutral-900 dark:text-neutral-100">
      {t(locale, "recipe.faq")}
    </h2>
    <div class="space-y-3">
      {faqs.map((faq) => (
        <details class="group rounded-lg border border-neutral-200 bg-white open:border-l-4 open:border-l-brand-primary dark:border-neutral-600 dark:bg-neutral-900/80">
          <summary class="flex cursor-pointer items-center justify-between px-5 py-4 font-ui font-medium text-neutral-800 hover:text-brand-primary dark:text-neutral-200">
            {faq.question}
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="h-5 w-5 shrink-0 text-neutral-400 transition-transform group-open:rotate-180 dark:text-neutral-500"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2"
            >
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </summary>
          <div class="border-t border-neutral-100 px-5 py-4 text-neutral-600 dark:border-neutral-700 dark:text-neutral-400">
            <p>{faq.answer}</p>
          </div>
        </details>
      ))}
    </div>
  </section>
)}
