# Make reporting freshness visible

Venue Reviews and Date Spots will show when DMD visited, published, and last checked them, with material changes added as dated updates. Dynamic local information changes too often for silent edits to preserve reader trust, so Claude schedules will create maintenance reminders rather than publishing changes automatically.
