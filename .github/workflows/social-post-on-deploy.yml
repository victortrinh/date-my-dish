name: Social Media Post on Deploy

on:
  push:
    branches: [main]
    paths:
      - "src/content/recipes/en/**/*.mdx"

concurrency:
  group: social-post-${{ github.sha }}
  cancel-in-progress: false

permissions:
  contents: write
  issues: write
  pull-requests: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  post:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 2
          token: ${{ secrets.PAT_TOKEN }}

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - name: Detect new recipes
        id: detect
        run: |
          NEW_RECIPES=$(git diff --name-only --diff-filter=A HEAD~1 -- 'src/content/recipes/en/*.mdx' || true)

          UNPOSTED=$(node -e "
            const fs = require('fs');
            const log = JSON.parse(fs.readFileSync('data/social-posts-log.json'));
            const recipes = fs.readdirSync('src/content/recipes/en')
              .filter(f => f.endsWith('.mdx'))
              .map(f => f.replace('.mdx',''));
            const missing = recipes.filter(r => !log[r]);
            if (missing.length) {
              console.log(missing.map(r => 'src/content/recipes/en/' + r + '.mdx').join('\n'));
            }
          ")

          ALL_NEW=$(printf '%s\n%s' "${NEW_RECIPES}" "${UNPOSTED}" | sort -u | grep -v '^$' || true)

          if [ -z "$ALL_NEW" ]; then
            echo "count=0" >> $GITHUB_OUTPUT
            echo "recipes=" >> $GITHUB_OUTPUT
          else
            echo "recipes<<EOF" >> $GITHUB_OUTPUT
            echo "$ALL_NEW" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
            echo "count=$(echo "$ALL_NEW" | wc -l | tr -d ' ')" >> $GITHUB_OUTPUT
          fi
          echo "Detected recipes to post: $ALL_NEW"

      - name: Post to social media
        if: steps.detect.outputs.count != '0'
        env:
          INSTAGRAM_ACCESS_TOKEN: ${{ secrets.INSTAGRAM_ACCESS_TOKEN }}
          INSTAGRAM_USER_ID: ${{ secrets.INSTAGRAM_USER_ID }}
          PINTEREST_ACCESS_TOKEN: ${{ secrets.PINTEREST_ACCESS_TOKEN }}
          PINTEREST_BOARD_ID: ${{ secrets.PINTEREST_BOARD_ID }}
        run: node scripts/social-post.mjs ${{ steps.detect.outputs.recipes }}

      - name: Create PR with post log
        if: steps.detect.outputs.count != '0'
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          BRANCH="chore/social-log-deploy-$(date +%Y%m%d-%H%M)"
          git checkout -b "$BRANCH"
          git add data/social-posts-log.json
          git diff --cached --quiet && exit 0
          git commit -m "chore: update social posts log"
          git push -u origin "$BRANCH"
          gh pr create \
            --title "chore: update social posts log" \
            --body "Automated: social media post log update after deploy." \
            --base main \
            --head "$BRANCH"
