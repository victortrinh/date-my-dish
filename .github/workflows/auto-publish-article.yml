name: Fetch Pending Article from Notion
on:
  schedule:
    - cron: '0 1 * * 1'  # Monday 1AM UTC (2h before scheduled task)
  workflow_dispatch:

concurrency:
  group: fetch-article
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - name: Fetch from Notion
        run: node scripts/fetch-notion-article.mjs
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}

      - name: Create PR with pending file
        if: success()
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          if [ -f notion/pending-article.json ]; then
            BRANCH="notion/pending-article-$(date +%Y%m%d)"
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git checkout -b "$BRANCH"
            git add notion/pending-article.json notion/published.json "src/assets/images/articles/**"
            git commit -m "chore: fetch pending article from Notion" || echo "No changes to commit"
            git push -u origin "$BRANCH"
            gh pr create \
              --title "chore: fetch pending article from Notion" \
              --body "Automated: fetched pending article data and images from Notion." \
              --base main \
              --head "$BRANCH"
          else
            echo "No article ready to publish"
          fi
