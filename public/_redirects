# Cloudflare Pages redirects
# Format: from  to  status

# www to apex — handle via Cloudflare DNS (CNAME redirect) instead of _redirects

# Root to English (static fallback for non-JS clients)
/  /en/  302

# Old WordPress URL redirects (add as you migrate each recipe)
# /chocolate-crepes/  /en/recipes/chocolate-crepes/  301
# /crepes-au-chocolat/  /fr/recettes/crepes-au-chocolat/  301
